# Tested release family documented by this tutorial (2026-07-16).
# Upgrade both pins together only after checking the Intel OpenVINO EP release matrix.
onnxruntime-openvino==1.24.1
# The Linux EP wheel bundles native OpenVINO libraries. Windows needs the
# matching standalone wheel; installing both distributions on Linux can make
# the dynamic loader select the wrong duplicate .so library.
openvino==2025.4.1; platform_system == "Windows"
# Exact top-level pins keep demo behavior stable. Transitive dependencies remain
# constrained by these packages rather than byte-for-byte locked.
onnx==1.22.0
numpy==2.3.5
