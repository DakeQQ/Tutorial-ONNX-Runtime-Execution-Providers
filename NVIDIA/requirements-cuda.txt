# ONNX Runtime 1.27 CUDA 13.0 runtime baseline for Windows/Linux x86-64.
# The ORT 1.27 cuda extra still references retired *-cu13 package names;
# use NVIDIA's current un-suffixed CUDA component packages through this meta-package.
onnxruntime-gpu==1.27.0
cuda-toolkit[cublas,cudart,cufft,curand,nvjitlink,nvrtc]==13.0.2
nvidia-cudnn-cu13==9.14.0.64

# Used only to generate the repository smoke-test model.
onnx==1.22.0
